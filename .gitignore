# data & large model files are gitignored (regenerable / large)...
data/
models/*
*.pt
# ...except the trained production model, which is shipped so the app runs on a fresh clone
!models/resnet50_colab_production.pth

# logs (clean metrics live in reports/train_history.csv, not the raw log)
*.log

# python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# os
.DS_Store
.gradio/
data_bundle.tar

# assistant/editor config — not part of the project
CLAUDE.md
.claude/

# data & models are large / not source — keep them out of git
data/
models/
*.pth
*.pt

# logs (clean metrics live in reports/train_history.csv, not the raw log)
*.log

# python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# os
.DS_Store
.gradio/
data_bundle.tar
